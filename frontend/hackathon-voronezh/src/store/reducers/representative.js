const initialState = {
    representative: ''
};

export const representativeReducer = (state = initialState, action) => {
    switch (action?.type) {
        default:
            return state;
    }
};
